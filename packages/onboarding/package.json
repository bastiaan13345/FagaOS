{
  "name": "@fagaos/onboarding",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "FagaOS onboarding and account-linking primitive (FAG-32). Defines the typed onboarding state machine, the account-link lifecycle, safe-default policy presets, and the human-readable scope explainer the control plane renders before consent.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run --root ../.."
  },
  "dependencies": {
    "@fagaos/agent-manifest": "*",
    "@fagaos/audit-log": "*",
    "@fagaos/connectors": "*",
    "@fagaos/core": "*",
    "@fagaos/policy": "*",
    "zod": "^3.23.0"
  }
}
